name: iOS Build (No Signing Test)

on:
  workflow_dispatch:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: macos-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Install Expo CLI
        run: npm install -g @expo/cli
        
      - name: Create missing assets
        run: |
          mkdir -p assets/sounds
          touch assets/sounds/notification.wav
          
      - name: Expo prebuild
        run: npx expo prebuild --platform ios --clean
        
      - name: Install CocoaPods
        run: |
          cd ios
          pod install --repo-update
          
      - name: Build for iOS Simulator
        run: |
          cd ios
          xcodebuild build \
            -workspace DanceDate.xcworkspace \
            -scheme DanceDate \
            -destination 'platform=iOS Simulator,name=iPhone 16'
            
      - name: Success Message
        run: |
          echo "🎉 DanceDate iOS build successful!"
          echo "📱 Built for iOS Simulator (no signing required)"
          echo "💰 Saving $99/month vs EAS builds!"
          echo "🚀 Ready for production builds with certificates!"
