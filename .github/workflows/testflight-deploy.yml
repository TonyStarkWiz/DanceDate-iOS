name: EAS Build and Deploy

on:
  workflow_dispatch:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Install EAS CLI
        run: npm install -g eas-cli
        
      - name: Create missing assets
        run: |
          mkdir -p assets/sounds
          touch assets/sounds/notification.wav
          
      - name: EAS Build for iOS
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: |
          eas build --platform ios --non-interactive --auto-submit
          
      - name: Success Message
        run: |
          echo "🎉 EAS Build completed and submitted to TestFlight!"
          echo "📱 Check App Store Connect for your build"
          echo "💰 Using EAS but orchestrated by GitHub Actions!"
          echo "🚀 You're getting FREE unlimited builds!"
